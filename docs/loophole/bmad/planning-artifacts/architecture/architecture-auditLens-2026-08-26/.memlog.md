---
scope: Рефакторинг агента лазеек в модульную Skill-архитектуру, включая интеграцию чата, экспорт и UI-модуль.
purpose: Контракт разработки и материалы согласования с архитектором и UX.
altitude: feature
updated: 2026-08-27T18:19
---

- (constraint) PRD: agent works through ReAct; default cap 20 with override; clarification does not consume an iteration; cap returns explained partial result.
- (constraint) SPEC: classifier is sixth independent skill; positive CCKS verdict automatically publishes a case; automatic processing never overwrites manual analyst or CCKS verdicts.
- (constraint) PRD: DB skill is read-only; Telegram secrets only come from environment; XLSX is capped at 10000; PDF has graceful fallback.
- (constraint) UX sources are authoritative for the loophole static UI and require self-hosted assets, inherited tokens, dark-theme synchronization, iframe-responsive layout, Russian labels, accessible controls and explicit error states.
- (decision) AD-1: agent.Agent is the target ReAct orchestration boundary; chat/graph.py adapts its existing chat and SSE contract. chat/tools_nanobot.py remains only as a compatibility shim until behavioral parity is proven, then is removed. Binds: migration path and runtime boundary. Prevents: permanent dual architectures and chat-contract breakage.
- (decision) AD-2: SkillRegistry reads SKILL.md metadata but enables only config.json allowlisted names mapped to registered Python entry points. Binds: skill discovery and loading. Prevents: arbitrary code execution from a directory scan.
- (decision) AD-3: persist redacted structured agent events in agent_audit_log: run lifecycle, tool calls, errors and final status; exclude full prompts, tokens and Telegram secrets. Binds: audit ownership and retention boundary. Prevents: non-auditable execution and secret leakage.
- (decision) AD-4: loophole-classifier is a sixth independent skill over existing classify.py; default batch is 50 records and configurable. Binds: classification invocation boundary. Prevents: classifier duplication and unbounded batch context.
- (decision) AD-9: scheduler service exclusively owns schedule lifecycle and persistent task state; skills submit commands and read typed status. Prevents duplicate schedulers and ambiguous runs.
- (decision) AD-10: audit events include run, user, workspace, redacted request summary, timestamps, duration, tool names and terminal result; never full prompts, payloads, raw LLM outputs or secrets.
- (decision) AD-11: versioned SSE progress events are sent within 15 seconds and on every long step, with run_id and machine code for localized UI.
- (decision) AD-12--AD-16 added after adversarial review: fail-closed authorization, transactional canonical audit schema, per-run factory and cutover gate, read-only DB defence in depth, and atomic manual verdict protection.
- (constraint) SPEC (2026-08-27): роль ЦК КС назначает администратор; агентский аудит хранится 14 дней и доступен только администратору модуля.
- (decision) AD-17: внешний Telegram-клиент является единственным владельцем Telegram-сессии и пишет только неизменяемые сырые объекты в отдельный ingestion-контур. AuditLens идемпотентно превращает их в источники AI-исследований; клиент не создаёт кейсы и не публикует каталог. Binds: OQ-13, Telegram monitoring. Prevents: прямую публикацию, утечку сессии и смешение владельцев данных.
- (decision) AD-18: изолированное AI-исследование, кандидат кейса, решение ЦК КС и опубликованный каталог имеют раздельное владение и связи. Публикация является детерминированной идемпотентной транзакцией только после положительного решения. Binds: CAP-3, CAP-4. Prevents: смешение исследования с каталогом и дубли при повторе.
- (decision) AD-19: авторизация ролей модуля fail-closed; администратор модуля управляет назначениями ЦК КС и единолично читает agent_audit_log. Retention job удаляет redacted audit events старше 14 дней и сам оставляет наблюдаемый результат. Binds: CAP-1, CAP-4, NFR audit. Prevents: несанкционированный доступ и бесконтрольное хранение.
- (decision) AD-20: внешний Telegram-клиент планирует и выполняет обход активных целей не реже раза в 24 часа, с durable lease, checkpoint и idempotent source key. При failover ровно один владелец сессии обрабатывает цель. Binds: Telegram lifecycle. Prevents: двойной сбор, пропуск инкремента и нарушение 24-hour SLO.
- (event) Product owner confirmed OQ-13 target: external Telegram ingestion boundary with idempotent AuditLens transformation into AI research sources.
- (decision) AD-21: AuditLens принимает только проверенный OIDC Principal и применяет DB least-privilege matrix: app, read-only DB skill, Telegram worker и retention job имеют раздельные роли. Worker имеет только TLS egress к Telegram и PostgreSQL ingestion boundary; inbound UI/shell access отсутствует. Binds: RBAC, DCL, deployment. Prevents: spoofed identity and cross-schema writes.
- (decision) AD-22: submitted case revision является immutable snapshot с evidence IDs, actor, expected version и idempotency key. Один conditional decision на snapshot и один publication mapping на confirmed decision обеспечиваются constraints; cross-case exact fingerprint links provenance to existing catalog record and never merges fuzzy matches automatically. Binds: lifecycle and publication. Prevents: concurrent opposing decisions and duplicate catalog entries.
- (decision) AD-23: Telegram worker writes an immutable redacted ingestion attempt/object journal, not agent_audit_log. AuditLens projector creates exactly-one redacted domain-audit summary keyed by worker_run_id and attempt_id. An ingestion object is PII-masked before persistence; unlinked objects expire after 14 days, while linked research evidence retains only redacted projection. Binds: audit and data minimization. Prevents: worker audit grants and raw PII persistence.
- (decision) AD-24: Telegram identity is versioned (logical account, peer, message, optional comment, revision). Ingestion projector uses claim state new/processing/linked/retryable_failed/quarantined and a unique origin link. Worker commits objects before checkpoint under target generation and lease fencing token; deactivate increments generation and invalidates lease. Binds: OQ-13 and failover. Prevents: stale writers, duplicate transform, poison loops and deactivated-target writes.
- (event) Reviewer gate findings integrated: harden lifecycle snapshot/decision/publication, worker audit/data flow, identity/DCL and ingestion fencing before repeated gate.
- (decision) AD-25: source evidence is append-only revisioned; submitted case revision snapshots evidence revision IDs and HMAC content fingerprints. CommandLedger namespaces command keys and enforces one submitted snapshot per case revision, one final decision per snapshot and one publication mapping per confirmed decision. CaseFingerprint v1 is deterministic versioned SHA-256 over case type, bank/product scope, category and sorted evidence HMACs; only exact matches link provenance, fuzzy matches never merge. Prevents snapshot drift, command collision and duplicate publication.
- (decision) AD-26: worker journal uses immutable attempt header, append-only batch events and one terminal event. A fenced transaction creates terminal event and unique audit outbox row; AuditLens projector inserts unique inbox/origin attempt audit summary in the same transaction. Session lease uses deployment telegram_session_id, while logical_account_id scopes target registry. Prevents mutable audit counters, duplicate relay and account/session confusion.
- (decision) AD-27: OIDC JWT supplies only subject identity; database workspace membership and role assignment are authoritative and rechecked for every request, mutating tool and scheduled execution. Global Telegram target has workspace subscriptions; evidence selection requires an active subscription. Scheduled DB contract pins named query ID/version, workspace, owner, result ownership and expiry; run-time reauthorization can skip a revoked owner. Prevents stale grants, cross-workspace evidence and scheduled privilege persistence.
- (decision) AD-28: PII processing is fail-closed. Current pii_mask is not a trust boundary: its replacement map stays only in memory. A versioned sanitizer must classify text and attachments; detector uncertainty, unsupported attachment or error produces quarantined metadata-only object, no persistence of body and no LLM/evidence path. Successful persistence uses redaction policy version and HMAC, never raw body. Prevents reversible PII, unknown attachments and false claims of masking.
- (event) Second reviewer-gate findings integrated: exact evidence snapshots and command ledger, immutable worker events with transactional relay, authoritative tenancy/authorization, and fail-closed PII policy.
- (decision) AD-29--AD-34: terminalization/reaper, payload-bound ledger and canonical fingerprint, subscription-CAS, delivery horizon, reproducible sanitization, scheduler/reconciliation contracts fixed after adversarial gate
- (decision) AD-20/29/31/33/34 clarified: initial checkpoint sentinel, subscription grant serial order preserving FR-12.4, keyed quarantine identity, active provisional Telegram registration, and bounded journal retention
- (decision) AD-35: TargetRegistryService atomically reserves target registration and workspace grant; telegram_worker only executes fenced resolve_provisional_target security-definer merge function
- (decision) AD-36: resolver promotion/merge terminalizes the same resolver_only attempt and its unique outbox; registration dereferences alias index and subscriptions may reference only canonical targets
- (decision) AD-37: Telegram skill registers only target per FR-4.2; a separate authorized TargetAccessService grants canonical workspace subscription
- (decision) AD-37 refined: only active module_admin with workspace target_subscription_manage may grant/revoke a provisional or resolved non-redirect target; evidence selection still needs successful ingestion
- (event) spine finalized
